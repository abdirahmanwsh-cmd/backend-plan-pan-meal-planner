# Copy this file to `.env` and edit values before running with docker-compose

POSTGRES_USER=meal_user
POSTGRES_PASSWORD=your_password
POSTGRES_DB=meal_planner

# When running with docker-compose the DB host will be `db` (service name)
DATABASE_URL=postgresql://meal_user:your_password@db:5432/meal_planner
